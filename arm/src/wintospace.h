#include <stdio.h> 
#include <math.h> 
#include <stdlib.h>
#define EPSILON 10;
#include <string.h>
#define allowerr 5;
#define koef 1;

//#define debugL

#ifndef START

#define START
int read (char* in, char* out);
int AreSame(float a, float b);
#endif
